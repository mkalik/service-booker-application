import React, { useState } from "react";
import { Form, Button, Alert } from "react-bootstrap";
import Auth from "../utils/auth";

const addTicketForm = () => {
  return (
    <>
      <Form>
        <Button>HelloMODAL</Button>
      </Form>
    </>
  );
};

export default addTicketForm;
